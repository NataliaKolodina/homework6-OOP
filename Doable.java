// package homework.homework6;

interface Doable { // создала базовый интерфейс, где создала общиe методы для всех экземпляров
                   // абстрактного класса

  void showAge();

  void eat();

  // добавила четыре метода, которые подходят не для всех сущностей базового
  // класса
  // каждый метод описала в отдельном интерфейсе, который наследует базовый
  // интерфейс
  // Doable

}
